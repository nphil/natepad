<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme — Compose handles all visual styling; this is the window chrome only -->
    <style name="Theme.NatePad" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>
</resources>
