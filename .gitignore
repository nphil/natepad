# Xcode build artifacts
build/
DerivedData/
*.xcarchive

# Android build artifacts (local compiles only — CI provides its own SDK)
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/.kotlin/

# Generated by xcodegen — we never commit this; CI regenerates it from project.yml
*.xcodeproj/
*.xcworkspace/
Info.plist

# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# macOS
.DS_Store

# User-specific Xcode noise (just in case)
xcuserdata/
*.xcuserstate
